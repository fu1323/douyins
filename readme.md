有点简陋的抖音直播流下载工具，本程序严重依赖ffmpeg，请提前安装并正确配置环境变量，抖音直播流大量使用hevc in flv封装，所以必须保证ffmpeg版本8.0以上，旧版会不识别封装报错! 程序会正确处理由于博主推流端抖动而造成的ffmpeg退出（会一直尝试拉流，尝试30次仍然404，就判断直播结束了，程序退出）  程序最后会在目录下生成一堆.ts文件（编号从小到大，中间缺号断开属于正常）  直播结束之后需要手动cat *.ts > xxx.ts然后再用ffmpeg 封装成mp4  直播地址获取需要抓包（浏览器/电脑端，手机有sslpinning抓不到包）